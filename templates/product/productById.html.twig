{% extends "base.html.twig" %}
{% block title %} SuperMarket | Product {{ productsById.title }}{% endblock %}
{% block body %}
<div class="flex justify-center flex-wrap mt-24 mb-8">

    
    <div class="mt-6 mr-6">
        <div class="block max-w-sm rounded-lg bg-white shadow-lg dark:bg-neutral-700">
            <img 
            class="rounded-t-lg" src="{{ productsById.img }}" alt="picture" />
            </a>
            <div class="p-6">
            <h5
                class="mb-2 text-xl font-medium leading-tight text-neutral-800 dark:text-neutral-50">
                {{ productsById.title }}
            </h5>
            <p class="mb-4 text-base text-neutral-600 dark:text-neutral-200">
                {{ productsById.description }}
            </p>
            <div class="flex justify-between">
                <h3 class="text-2xl mb-6">
                <strong>$ {{ productsById.price }}</strong>
                </h3>
                <p class="text-base text-neutral-600 dark:text-neutral-200">
                    quantity: {{ productsById.quantity }}
                </p>
            </div>
            </div>
        </div>
    </div>

</div>
{% endblock %}